import { describe, expect, it } from "vitest";

// Placeholder so `vitest run` has a suite on the empty scaffold. The real
// build-output tests (page metadata, link resolution, HTML validity) arrive
// with the layout and content stages.
describe("scaffold", () => {
  it("has a working test runner", () => {
    expect(true).toBe(true);
  });
});
