---
# Copy this file to start a new project page, then remove `published: false`
# so it goes live. Only the keys below are read — keep the frontmatter minimal.
title: Project Title
permalink: /mlsec/yourproject # the live URL: pick /mlsec, /platsec, or /others
# redirect_from:               # optional — old URLs that should redirect here
#   - /oldpath/
published: false
---

# Project Title

## Overview

One or two paragraphs describing the project.

## Conference/journal paper publications

<!-- TODO(bibliography): render papers with selected=yourkey -->

To list a paper here, add it once to `papers.bib` with a `selected={yourkey}`
field; the bibliography component renders every entry that matches. Nothing else
needs editing.

## Source code

- [Repository name](https://github.com/ssg-research/yourrepo)
