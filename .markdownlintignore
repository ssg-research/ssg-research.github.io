# Build output and dependencies.
node_modules/
dist/
.astro/

# Verbatim-ported page/project content (incl. the contributor template). Not
# linted so first-pass porting preserves intentional source quirks (multiple
# H1s, raw HTML, double blank lines).
src/content/

# Local-only working notes (not part of the site).
agent_docs/
