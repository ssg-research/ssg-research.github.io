_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
vendor

# Astro / Node toolchain (added for the Jekyll → Astro migration)
node_modules/
dist/
.astro/

# local working notes for maintenance sessions (not part of the site)
agent_docs/
.playwright-mcp/
